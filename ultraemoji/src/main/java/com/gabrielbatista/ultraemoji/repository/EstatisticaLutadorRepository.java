package com.gabrielbatista.ultraemoji.repository;

import com.gabrielbatista.ultraemoji.domain.EstatisticaLutador;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EstatisticaLutadorRepository extends JpaRepository<EstatisticaLutador, Long> {
}
